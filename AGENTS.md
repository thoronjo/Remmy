# Project Agent Rules

Always use `$security-baseline` for any implementation, refactor, dependency change, deployment change, or code review.

Security baseline is mandatory by default. If a task conflicts with it, flag the violated rule and require explicit user override before proceeding.
